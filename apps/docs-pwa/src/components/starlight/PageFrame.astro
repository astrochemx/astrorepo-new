---
import Default from '@astrojs/starlight/components/PageFrame.astro';

import { usePwaPrompt } from '../../../astro.config.ts';
import ReloadPrompt from '../custom/ReloadPrompt/ReloadPrompt.astro';
---

<Default>
  <slot name="header" slot="header" />
  <slot name="sidebar" slot="sidebar" />
  <slot />
</Default>
{usePwaPrompt && <ReloadPrompt />}
